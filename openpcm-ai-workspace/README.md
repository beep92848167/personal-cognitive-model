# OpenPCM AI Workspace

This folder contains persistent context intended for a Developer API
workspace.

Load these files into the workspace so the AI retains project context
with minimal prompt overhead.
