# ARCHITECTURAL DECISIONS

-   Requirement-first engineering.
-   Tests before commit.
-   Sync metadata stored in `.openpcm-sync.json`.
-   Android remains the ultimate deployment target.
-   Windows uses PCM Runner instead of recreating the Termux `u`
    command.
-   Browser automation intentionally deferred until workflow reliability
    is proven.
