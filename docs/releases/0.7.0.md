# OpenPCM Mobile 0.7.0

## Summary

Introduces the first shared Core module and moves evidence domain logic out of UI-only code.

## Changed

- Added `src/core/evidence.js`.
- Added `src/core/README.md`.
- Added `project.json`.
- Updated `index.html` to load the Core module before `app.js`.
- Refactored `app.js` to call `OpenPCMCore`.
- Refactored evidence tests to exercise shared production logic.
- Added storage helper tests.

## Breaking changes

None intended.

## Manual verification

Open `http://localhost:8080/tests/test.html`.

Expected: all tests pass.
