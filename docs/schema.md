# Schema Notes

This repository is intentionally modular.

- `profile/` describes the user.
- `media/` describes categories of media.
- `knowledge/` stores reusable evidence, features, calibration, and counterfactuals.
- `schemas/` contains validation schemas.
- `tools/` contains helper scripts.

Future versions should add stable IDs to every media item and feature.
